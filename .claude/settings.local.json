{
  "mcpServers": {
    "trade-signal": {
      "command": "docker",
      "args": ["compose", "exec", "-T", "api", "python", "mcp/server.py"],
      "env": {
        "API_BASE": "http://localhost:8000"
      }
    }
  }
}
